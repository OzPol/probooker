// Header.tsx
// Displays the top navigation/header bar of the application
import React from 'react';

const Header: React.FC = () => {
  return <header>This is the Header</header>;
};

export default Header;
